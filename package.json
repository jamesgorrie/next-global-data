{
  "scripts": {
    "start": "ts-node --project tsconfig.server.json server",
    "build": "next build",
    "startProd": "yarn build && NODE_ENV=production ts-node --project tsconfig.server.json server"
  },
  "dependencies": {
    "isomorphic-unfetch": "^3.1.0",
    "next": "^11.1.2",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "ts-node": "^10.2.1"
  },
  "devDependencies": {
    "@types/react": "^17.0.24",
    "typescript": "^4.4.3"
  }
}
